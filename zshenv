# Should be copied to ~/.zshenv.
# All it does is set the ZDOTDIR variable, then zsh will process
# the files in there.
ZDOTDIR=$HOME/.config/dotfiles/zsh
source $ZDOTDIR/.zshenv

# Add local configuration here, if needed...
